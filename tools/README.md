# Tools

Extraction, reconstruction, comparison, verification, and build-support tools for the disassembly project live here.

Planned areas:

- `extract/` — ROM structure and asset extraction
- `reconstruct/` — conversion into editable disassembly-source formats
- `compare/` — cross-version and bank-level comparison
- `verify/` — byte-exact build and hash verification
